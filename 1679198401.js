var number = [5, 6, 8, 2];
var age = [8, 10, 5, 4];

function myFunction(number, age) {
  for (var i = 0; i < opps.length; i++) {
console.log("Testing");
 if (number[i] === age[i]) {
 console.log("Common :", age[i]);
    break;
      }
   }
   console.log(number[i]);
   console.log(age[i]);
  return true;
}
myFunction(number, age);